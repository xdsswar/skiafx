/*
 * jdk.jsobject — JavaScript interop bridge (used by WebView, but the
 * module itself is independent and small).
 */

plugins {
    id 'skiafx.java-conventions'
    id 'skiafx.module-conventions'
    id 'skiafx.upstream-sync'
    id 'skiafx.sdk-conventions'
}

upstream {
    trackedFromJfxMaster 'modules/jdk.jsobject'
}

description = 'JavaScript interop bridge (netscape.javascript).'
